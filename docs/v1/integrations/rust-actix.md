# Rust Actix-web

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```rust
// Serve static index + JS that boots HTMLEditor.
// Actix handler accepts POST body with HTML from editor.getHtml().
```
