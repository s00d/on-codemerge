# Ruby on Rails

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```erb
<%# same pattern: host div + Stimulus/JS controller that creates HTMLEditor and syncs getHtml() before submit %>
```
