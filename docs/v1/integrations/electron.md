# Electron

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```js
// In renderer: new HTMLEditor(document.getElementById('app')); editor.use(new ToolbarPlugin());
// Load on-codemerge CSS in the BrowserWindow page.
```
