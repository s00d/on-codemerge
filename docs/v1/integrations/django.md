# Django

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```html
<!-- template: div#editor + CSRF form field -->
<!-- static JS: new HTMLEditor(...); on submit copy editor.getHtml() into hidden input -->
```
