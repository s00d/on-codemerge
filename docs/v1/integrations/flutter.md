# Flutter

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```dart
// Embed via WebView loading a local/remote HTML page that boots HTMLEditor.
// Bridge getHtml()/setHtml() with JS channels if you need Dart ↔ editor sync.
```
