# Angular

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```ts
// AfterViewInit: new HTMLEditor(this.host.nativeElement); editor.use(new ToolbarPlugin());
// OnDestroy: editor.destroy();
// Import on-codemerge CSS in angular.json or global styles.
```
