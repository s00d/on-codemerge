# Next.js

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```tsx
'use client';
// Same as React: mount HTMLEditor in useEffect on a div ref.
// Import CSS from a client component or global stylesheet.
```
