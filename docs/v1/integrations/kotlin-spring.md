# Kotlin Spring Boot

> Archive: v1 API. Current guides: [Integrate](/integrate/).

Boot **HTMLEditor** on a host element, register plugins, import CSS.

## Example

```kotlin
// Same as Spring: static/Thymeleaf host page + JS HTMLEditor; POST HTML for persistence.
```
